from pathlib import Path


def cut(
    input_path: Path, output_path: Path, start_seconds: float, end_seconds: float
) -> None:
    """Trim media file to scheduled start and end seconds."""
    raise NotImplementedError("cut: Phase 3 implementation pending")
