from pathlib import Path


def publish(input_path: Path, output_path: Path) -> None:
    """Publish media to the destination path."""
    raise NotImplementedError("publish: Phase 3 implementation pending")
