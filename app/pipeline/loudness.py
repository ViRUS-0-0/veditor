from pathlib import Path


def normalize_loudness(input_path: Path, output_path: Path) -> None:
    """Normalize audio loudness via PyAV audio filter graph."""
    raise NotImplementedError("loudness: Phase 3 implementation pending")
